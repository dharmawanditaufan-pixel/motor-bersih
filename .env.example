# Environment Variables for Motor Bersih POS

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=motowash_db

# JWT Secret Key (change in production!)
JWT_SECRET=motor-bersih-secret-key-2026-change-this-in-production

# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration
CORS_ORIGIN=http://localhost

# Railway.com MySQL (when deployed)
# DB_HOST=${MYSQLHOST}
# DB_USER=${MYSQLUSER}
# DB_PASS=${MYSQLPASSWORD}
# DB_NAME=${MYSQLDATABASE}
# DB_PORT=${MYSQLPORT}
